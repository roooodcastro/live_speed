<%= title t('.title') %>

<%= vue_component('MatchList', props: {
    'player_id':  current_player_id,
    ':matches':    @matches.to_json,
    title:         'lobby.show.matches_waiting',
    'empty-title': 'lobby.show.no_matches_waiting',
}) %>
